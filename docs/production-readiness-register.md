# Production readiness register

This register is deliberately fail-closed. `OPEN` items prevent autonomous bid
release even when repository tests pass.

| Gate | Current evidence | Status | Required owner/evidence |
|---|---|---|---|
| Full end-to-end controlled calculation | intake through passport, BoQ/quantity, scope, nomenclature/pricing/RFQ, typed logistics/mobilisation/contract finance, contract, risk, calculation, controlled scenario execution, independent snapshot/release, signed audit export, actual comparison and calibration approval are application-connected; a signed generic delivery/inbox boundary exists; qualified external extraction/normative/market/route/treasury/business adapters and scenario business qualification remain open | **PARTIAL** |
| Reproducibility | server-generated canonical Decimal inputs, approved policy/version binding, content hashes, independently checked immutable snapshot, snapshot/run replay, and target-specific release gate hashes are implemented; production source qualification remains external | CORE IMPLEMENTED |
| Independent machine validation | separate recalculation path and mismatch/double-count tests | CORE IMPLEMENTED |
| Expert/four-eyes controls | version-bound tasks cover critical changes, analogues, price spread, contract impacts, reserves and calibration; organisation policy qualification absent | **PARTIAL** |
| No unexplained sums | every calculation input must exactly cover a planned BoQ component and reproduce current quantity/category/basis/source/rate/currency/unit; qualified normative and external sources remain absent | **PARTIAL** |
| No unregistered manual changes | immutable/revisioned observations, passport, quantities, contract, risk and actuals plus audited approval decisions; governed UI supports exact-manifest four-eyes document-set confirmation, source-level conflict resolution, policy/current-document-bound manual evidence correction, policy-bound quantity revision, price registration/normalization/triangulation, server-candidate calculation and hash-bound release without browser-entered financial values; manual evidence remains immutable/unverified until a dedicated independent review creates a separate derived observation; quantity changes retain immutable before/after state, exact source evidence, independent approval, author-only server-side application and PostgreSQL guards; calculation and release commands bind exact server context and PostgreSQL makes runs, atomic inputs, scenarios and release decisions append-only; PostgreSQL also protects approval task scope/transitions and requires one append-only decision record matching every terminal task; generic decisions cannot bypass dedicated controls, while stale context, reused changes and broken approval linkage fail closed; other controlled mutation surfaces and complete UI/process qualification are absent | **PARTIAL** |
| Dangerous case blocking | negative tests cover new-revision invalidation, stale/tampered snapshots and runs, controlled-version content/audit/four-eyes drift, direct approved insertion, illegal lifecycle transition, deletion and duplicate-kind ambiguity, conflict resolution, evidence independence, manual-evidence self-review/policy substitution/document-set drift/repeated decision/nested float, stale attestations, scope/quantity, policy-versioned price-normalization replay, explicit rounding/overflow, tampered normalized amounts and price decisions, commercially incomplete price exclusion/RFQ, contract, risk, component coverage, invented sources, client input/policy substitution, stale calculation/release hashes, arithmetic and release policy; business qualification rejects missing references, creator evaluation, self-review, rounded-threshold masking, package substitution and incomplete production evidence; non-business gate evidence rejects external substitution for load/recovery, self-review, signature/hash/object/result mismatch, expiry and revocation; recovery qualification rejects missing/corrupt snapshot objects, dangling atomic-input bases, and modified approved profiles; real PostgreSQL guards protect governed versions, approval tasks/records, price, calculation, release and qualification evidence; real production evidence remains absent | **PARTIAL** |
| Historical accuracy | governed closed-population dataset/profile, verified-actual and four-eyes audit reproduction, forecast locking, exact per-case/MAPE/bias evaluation, immutable result/package and production-gate revalidation are implemented; no organisation-approved historical population, thresholds, completed campaign or measured metrics supplied | **PARTIAL - EVIDENCE OPEN** |
| Blind estimator comparison | post-lock two-step professional evidence, blindness/no-bid-authority attestations, preparer/reviewer/system-creator separation, minimum independence-domain rules, exact metrics and production-gate linkage are implemented; no completed independent professional comparison exists | **PARTIAL - EVIDENCE OPEN** |
| Parallel operation | post-lock parallel reference workflow, controlled minimum calendar span, exact mode metrics, discrepancy analysis and immutable approval package are implemented; representative parallel operation has not been run | **PARTIAL - EVIDENCE OPEN** |
| Normative engine | interface exists; no licensed qualified adapter configured | **OPEN** |
| Market/RFQ connectors | interfaces exist; credentials/source agreements not supplied | **OPEN** |
| OCR + independent visual extraction | interfaces/reconciliation exist; qualified providers absent | **OPEN** |
| ERP/DMS/BI integrations | signed Ed25519 envelope/receipt contracts, HTTPS allowlisted adapter, qualification-bound organization/topic/key identity, immutable inbox and delivery evidence, dedupe, lease/retry/dead-letter and controlled replay are implemented and tested; real ERP/DMS/BI mappings, endpoints, credentials, schedulers, monitoring and qualification evidence are absent | **PARTIAL - PRODUCTION BLOCKER** |
| Detailed logistics/mobilisation | typed capacity-constrained transport, handling, storage, ancillary and mobilisation models; exact observation-value binding; controlled completeness/rounding; independent recalculation; mandatory four-eyes; immutable PostgreSQL revisions; BoQ/calculation/lineage integration are implemented and tested; real route/rate feeds and business qualification are absent | **PARTIAL** |
| Detailed contract finance | dated signed cash flows, piecewise funding rates, guarantee fees, day-count policy, independent day-by-day recalculation, required contract-term lineage, derived contract-impact and snapshot integration are implemented and tested; treasury feeds, indexation/FX policy data and business qualification are absent | **PARTIAL** |
| Operational scenario modelling | approved-policy scenarios execute from replay-verified fixed snapshots, persist results, independently recalculate, and are replayed again for the bounded comparison read model; the operator UI shows the exact snapshot/policy/evidence overrides, base/scenario/delta, integrity failures and explicit truncation, while submitting only snapshot/scenario identifiers; organisation-approved scenario definitions and business qualification remain absent | **PARTIAL** |
| Operator and reviewer UI | production-built OIDC PKCE interface exposes role-filtered portfolio, work queue, project hard stops, indicators, record lineage and audit history; controlled project registration, quarantined upload/receipt polling, exact-manifest document-set confirmation, source-level conflict resolution, governed manual evidence entry/review, qualified independent reconciliation, current-set BoQ authoring and stale-safe four-eyes review, scope completeness execution, deterministic catalog assessment, analogue proposal/finalization review, quantity revision, price registration/normalization/triangulation, expert decisions, server-candidate calculation, controlled scenario comparison/execution and target-specific release use explicit attestations and stable idempotency; reconciliation exposes exact source/qualification/domain blockers and submits no merged value; BoQ and nomenclature operations reproduce the current confirmed manifest, reject old-set evidence and bind full controlled-version audit chains; the browser never submits a nomenclature result class; manual evidence binds the server-selected approved policy and current document revision/hash, preserves exact values without floating point and requires a separate reviewer before a derived verified observation exists; quantity revision binds the exact current quantity, confirmed document set, approved policy/rules, immutable before/after state and source observations, requires policy-assigned four-eyes review for critical changes, prevents the reviewer from applying, and lets only the author apply the server-held after-state; price registration takes only a verified observation ID, exposes full commercial bases, permits only current approved policy references, uses server-side policy rounding/replay and opens RFQ on incomplete triangulation; calculation submits no financial value, scenario execution submits no override/financial value, and release binds the complete current gate hash/project version; conflict/manual-evidence/BoQ/analogue decisions retain dedicated version/evidence/four-eyes guards; upload UI preserves quarantine/evidence separation and server limits; same-origin navigation, exact decimal rendering, route-level code splitting, security headers, frontend tests, local clean-console desktop/mobile browser requalification and container build are implemented; the narrow-screen attribute matrix and scenario cards preserve all technical and financial comparison fields. Specialist maintenance surfaces, formal accessibility evidence, role-based UAT, independent browser qualification and business qualification remain absent | **PARTIAL** |
| Export packages | released fixed snapshots produce deterministic content-addressed Ed25519-signed JSON packages with lineage, versions, approvals, workflow, release decision and verified audit chain; generic signed delivery exists, but an approved export receiver mapping, public-key trust registry and business qualification are absent | **PARTIAL** |
| Mutation reliability/outbox | all mutating APIs atomically persist actor-scoped idempotency, business writes, audit, universal outbox and replay response; signed outbound delivery preserves an external dedupe key and signed receipt; inbound messages are immutable and processed through leased generation records; retries, collision rejection, dead letters and replay are tested; production scheduler, real downstream inbox conformance, alerts and drills remain absent | **PARTIAL - PRODUCTION BLOCKER** |
| Load test | a governed four-eyes `load_test_profile`, read-only bounded runner, exact request cap, overall/per-endpoint owner-defined SLO evaluation, tamper-evident result and negative tests are implemented; atomic PostgreSQL actor/organisation quotas independently classify read/mutation/upload traffic and fail closed on policy/store failure; the production gate requires a live evidence-registry package that reproduces the approved source profile, exact result artifact, build/environment, independent approval, age and audit chain; no organisation-approved thresholds, representative corpus/workload, independent ingress/upload concurrency controls, abuse/soak run, infrastructure telemetry or real approval has been supplied | **PARTIAL - EVIDENCE OPEN** |
| Malware scanning/sandbox | streamed separate quarantine, immutable qualification-bound exact-hash scan results, bounded archive streams, leased outbox dispatch, bounded retry/dead-letter/audited replay, and short-transaction worker parsing are implemented and tested; real scanner provider, production scheduler, disposable runtime limits, network policy and qualification evidence are absent | **PARTIAL - PRODUCTION BLOCKER** |
| Project access/information barriers | versioned owner-managed membership, action-specific project roles, same-tenant non-disclosure, qualified service identities, audited revocation, immutable PostgreSQL history, role-set/mask consistency guards, and authorization-before-pagination portfolio/work-queue queries are implemented and migration-tested on SQLite and PostgreSQL; IdP lifecycle, periodic recertification, break-glass recovery and optional RLS evidence are absent | **PARTIAL** |
| Audit integrity/WORM | versioned HMAC keys, verified legacy migration, immutable global checkpoints, four-eyes Ed25519 external receipts, full current-chain/anchored-terminal verification, and live WORM readiness enforcement are implemented and negatively tested; production provider, real bucket-policy evidence, schedules, alerts and tamper drill are absent | **PARTIAL - PRODUCTION BLOCKER** |
| Security review | build/environment-scoped external-review profiles, retained content-addressed report objects, governed Ed25519 attester keys, independent package approval, expiry/revocation and release-time validation are implemented; request bodies are bounded and authenticated actor/organisation quotas are distributed and fail closed, but independent edge/unauthenticated/upload-concurrency controls and no approved full-scope production threat/configuration/penetration review package have been supplied | **PARTIAL - EVIDENCE OPEN** |
| Backup and point-in-time recovery | governed four-eyes recovery profiles and exercise manifests, exact build/schema/object/document/snapshot/run/input/basis/scenario/export/audit/checkpoint/anchor verification, deterministic all-snapshot/scenario and named-golden replay, approved-RPO/RTO comparison using unrounded values, exclusive self-verifying content-hashed results and corruption/profile-tamper negative tests are implemented; the production gate now requires a live evidence-registry package reproducing that result and its retained artifact; no real organisation backup, restored PostgreSQL/WAL/object-store versions, alternate account or independent exercise evidence exists | **PARTIAL - PRODUCTION EVIDENCE OPEN** |
| Disaster recovery | isolated-environment binding and fail-closed recovery qualification are implemented; target RPO/RTO, alternate environment, failover/failback, identity/secrets/connector proof, monitoring continuity and independent sign-off have not been approved or exercised | **PARTIAL - PRODUCTION EVIDENCE OPEN** |
| Methodology approval | an externally attested, content-addressed, independently approved, expiring/revocable methodology package is required and live-validated at release; the mechanism exists, but the organisation has not approved the methodology or supplied the package | **PARTIAL - EVIDENCE OPEN** |
| Catalog/rule/model owners | roles exist; named people not supplied | **OPEN** |
| User training | materials and completion records absent | **OPEN** |
| Operating regulations | draft runbook exists; formal approval absent | **OPEN** |

## Consequence

The repository must not be labelled production-ready and the
`production_qualification` controlled version must not be approved until every
required gate has named evidence, date, environment, reviewer, and owner. The
release policy also requires a qualified normative adapter and an approved
production qualification version.
